import styles from './SampleBadge.module.scss';

/* eslint-disable-next-line */
export interface SampleBadgeProps {}

export const SampleBadge = (props: SampleBadgeProps) => {
  return <div className={styles['root']}>SampleBadge3</div>;
};
