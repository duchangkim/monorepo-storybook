import { projectsDuseExamplePcUtil } from './projects-duse-example-pc-util';

describe('projectsDuseExamplePcUtil', () => {
  it('should work', () => {
    expect(projectsDuseExamplePcUtil()).toEqual('projects-duse-example-pc-util');
  });
});
