import { render } from '@testing-library/react';

import { <%= fileName %> } from './<%= fileName %>';

describe('UIComponent <%= fileName %>', () => {
  it('성공적으로 렌더링 되었는가?', () => {
    const { baseElement } = render(<<%= fileName %> />);

    expect(baseElement).toBeTruthy();
  })
});
